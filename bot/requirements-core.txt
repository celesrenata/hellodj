discord.py[voice]
wavelink>=3.5.0
python-dotenv
aiohttp
cryptography>=42.0.0

# Voice activation - core/light deps (no torch/transformers/spacy)
discord-ext-voice-recv>=0.5.2a179
onnxruntime>=1.18.0
numpy>=1.26.0

# General query APIs
openai>=1.0.0

# Cloud STT/TTS — AWS Bedrock (Amazon Transcribe + Polly)
# boto3 is a lightweight AWS SDK client with no heavy dependencies.
boto3>=1.28.0

# Tidal music-video streaming (/stream)
# yt-dlp is a pure-Python package (NixOS-clean) used to download Tidal/HLS
# video streams into a Discord-embeddable file.
yt-dlp>=2025.1.15
