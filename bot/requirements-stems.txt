# Optional heavy AI stem-separation stack for /filter stems isolate drums|bass|melody
#
# WARNING: These are HEAVY dependencies (torch/onnx/tensorflow, multi-GB) and are
# NOT installed by default. Installing them is optional and separate from the
# core image so pushes stay small. See bot/stems.py and bot/.env.example
# (STEM_MODEL) for the model hook.
#
# Even with these installed, the current streaming architecture cannot inject a
# locally separated stem into Lavalink playback — only the vocals-isolation
# (instrumental) karaoke fallback works today. This file documents the seam for
# a future AI integration.
#
# Pick one backend (do not install all three):
#   - demucs:  torch-based, best quality (Meta)
#   - spleeter: tensorflow-based (Deezer)
#   - onnxruntime: for a pre-exported ONNX separation model
demucs>=1.0.0
# spleeter>=2.0.0
# onnxruntime>=1.18.0
