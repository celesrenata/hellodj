# Runtime dependencies for the voice-pipeline component (R15.1: independently
# deployable). The local wake word ONNX model is the ONLY on-box AI; STT,
# intent, and TTS are delegated to Amazon Bedrock / Transcribe / Polly reached
# via boto3 using the pod's IAM task role (no static keys). Discord voice
# (opus) is consumed via bot-core, so there is no discord/voice dependency.
#
# Explicitly NOT present (removed self-hosted AI per the design):
#   Kokoro TTS, faster-whisper / CTranslate2, self-hosted LLM, Speaches.
onnxruntime>=1.17
boto3>=1.34
numpy>=1.26
